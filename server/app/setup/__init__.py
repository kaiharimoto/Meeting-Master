# First-run setup mode: a loopback-only, unauthenticated wizard the server
# serves to itself so the home PC can be configured (email, dependencies,
# connection code) without editing server.env or touching PowerShell.
